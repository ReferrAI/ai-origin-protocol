# Architecture (High Level)

AIPP defines a signed `intent_token` that can flow:

AI surface → retailer resolve → (optional) aggregate outcome signals

The protocol is designed to be:
- Zero-PII
- Short-lived (TTL)
- Verifiable (signatures)
- Interoperable (optional adoption)
